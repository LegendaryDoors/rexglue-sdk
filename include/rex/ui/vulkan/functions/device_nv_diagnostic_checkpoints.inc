// VK_NV_device_diagnostic_checkpoints - survives VK_ERROR_DEVICE_LOST, so it
// can name what the GPU was actually executing when it died.
XE_UI_VULKAN_FUNCTION(vkCmdSetCheckpointNV)
XE_UI_VULKAN_FUNCTION(vkGetQueueCheckpointDataNV)
